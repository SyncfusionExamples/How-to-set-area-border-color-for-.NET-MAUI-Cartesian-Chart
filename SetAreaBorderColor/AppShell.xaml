<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="SetAreaBorderColor.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:SetAreaBorderColor"
    Title="SetAreaBorderColor"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent
        Title="Using GraphicsView"
        ContentTemplate="{DataTemplate local:UsingBorder}"
        Route="UsingBorder" />

        <ShellContent
        Title="Using GraphicsView"
        ContentTemplate="{DataTemplate local:UsingGraphicsView}"
        Route="UsingGraphicsView" />

    </TabBar>
    


</Shell>
